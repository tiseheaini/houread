<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "标题" %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.kindeditor :body, :width => 720, :height => 300 %>
  </div>
  <div class="actions">
    <%= f.submit "发布", :class => "btn" %>
  </div>
<% end %>
